# ניתוח V13 ללוג האחרון מהשרת - 2026-07-07 12:38

## קובץ

- לוג: `cloud_logs_20260707_latest_check/log_20260707_123834_22BD4367.kslog`
- הועלה: `2026-07-07T09:39:39.845Z` (`12:39:39` בישראל)
- מנוע פעיל בלוג: `v13-pure`
- משך: `61.12s`
- motion: `12,119` דגימות, כ־`198.3Hz`
- Absolute Altitude: `63` שינויי ערך
- GPS: אין רשומות GPS בלוג (`tag 7` לא מופיע)

## תוצאת V13

Replay עם V13 הנוכחי:

```text
Jumps: 0
```

גם בלוג האירועים מהשעון אין `CANDIDATE` ואין `JUMP`; יש רק:

```text
SYNC marker
v13 altitudeSource=absoluteAltitude
JumpDetector(v13) endSession flush lateJumps=0
```

## האם שתי הקפיצות נראות בלוג?

כן, לפי Absolute Altitude רואים שתי עליות:

### קפיצה 1

```text
t=11.161572 alt=10.003
t=12.162297 alt=11.320
rise=1.317m
dt=1.000725s
```

אחר כך יש ירידה חזקה:

```text
t=13.163023 alt=9.959
t=14.158744 alt=8.951
```

### קפיצה 2

```text
t=21.158807 alt=9.902
t=22.159531 alt=11.468
rise=1.566m
dt=1.000724s
```

אחר כך יש ירידה:

```text
t=23.160254 alt=10.926
t=24.160977 alt=9.382
```

## החסם הראשון: חלון קפיצה קשיח מדי

V13 עם `minRiseM=1.0` משתמש בחלון קפיצה של `1.0s`.

שתי העליות הן בערך `1.0007s`, כלומר רק `0.0007s` מעבר לחלון.
בגלל שהקוד חותך את `takeoffWindow` לפי `p.t - 1.0`, הדגימה הראשונה יוצאת מהחלון רגע לפני בדיקת העלייה.

לכן V13 לא פתח אפילו `CANDIDATE`.

אם מוסיפים tolerance קטן של `0.02s` לחלון, שתי העליות היו עוברות את תנאי הקפיצה של `1.0m`.

## החסם השני: סף 1.5/2.0

אם המסך היה מוגדר על `1.5m` או `2.0m`:

- קפיצה 1 לא עוברת: `1.317m`.
- קפיצה 2 עוברת גובה `1.566m`, אבל לא עוברת את חלון הזמן של `1.5s`, כי בדגימה הבאה הגובה כבר יורד.

כלומר שתי הקפיצות נראות רק עבור סף `1.0m`.

## החסם השלישי: נחיתה יציבה

גם אם פותחים קפיצה עם tolerance קטן:

- קפיצה 1 לא מקבלת חלון נחיתה יציב מיד אחרי הירידה. חלון הנחיתה הראשון:

```text
13.163023 -> 15.159469
values=[9.959, 8.951, 9.718]
delta=0.241m
range=1.008m
```

`delta` עובר, אבל `range` נכשל כי הסף הוא `0.6m`.

- קפיצה 2 מקבלת חלון נחיתה ראשון:

```text
26.162421 -> 28.158862
values=[9.410, 9.655, 9.805]
delta=0.395m
range=0.395m
```

`range` עובר, אבל `delta` נכשל כי הסף הוא `0.25m`.

החלון היציב הראשון שבאמת עובר מגיע רק סביב:

```text
35.163910 -> 37.160348
values=[10.519, 10.517, 10.617]
delta=0.098m
range=0.100m
```

זה מאוחר מדי: עבור קפיצה 2 זה נותן airtime של בערך `14s`, מעל `maxAirtimeSec=12`.

## מסקנה

אפשר לראות שתי קפיצות בלוג, אבל V13 חסם אותן בפועל בגלל:

1. חלון קפיצה של `1.0s` בלי tolerance מול cadence של Absolute Altitude שהוא בפועל `1.0007s`.
2. ספי `1.5m/2.0m` לא מתאימים ללוג הזה; רק `1.0m` יכול לראות את שתיהן.
3. תנאי נחיתה יציבה קשיח מדי לים/תנועה בלוג הזה, במיוחד `landingStableRangeM=0.6` לקפיצה 1 ו־`landingStableDeltaM=0.25` לקפיצה 2.
4. אין GPS בלוג, אז אי אפשר לחשב מהירות/מרחק אמיתיים מהקובץ הזה.
